<UserControl x:Class="JadeControls.SearchResultsControl.SearchResultsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="800">
    <DockPanel Margin="0">
        <ToolBar DockPanel.Dock="Top">
            <ComboBox ItemsSource="{Binding Searches}">
            </ComboBox>
        </ToolBar>        
        <ListBox ItemsSource="{Binding CurrentSearch.Items}"
                 SelectedItem="{Binding CurrentSearch.SelectedItem}"
                 MouseDoubleClick="ListBox_MouseDoubleClick"/>
    </DockPanel>
</UserControl>
