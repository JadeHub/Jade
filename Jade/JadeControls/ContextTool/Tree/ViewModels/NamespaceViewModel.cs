using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using JadeCore.CppSymbols2;

namespace JadeControls.ContextTool
{/*
    public class NamespaceViewModel : TreeItemBase
    {
        public NamespaceViewModel(ITreeItem parent, NamespaceSymbol symbol)
            :base(parent, symbol.Spelling)
        {
            foreach(ClassDeclarationSymbol c in symbol.Classes)
            {
                Children.Add(Factory.MakeTreeItem(this, c.Cursor));
            }
        }

        public override string TypeChar { get { return "N"; } }
    }*/
}
